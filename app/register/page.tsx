import { AuthShell } from '@/components/auth-shell'

export const metadata = { title: 'Create account — VaaniDoc', description: 'Join VaaniDoc for multilingual AI-powered healthcare assistance.' }

export default function RegisterPage() { return <AuthShell mode="register" /> }
